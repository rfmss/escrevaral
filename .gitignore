# ── Design / binários ────────────────────────────────────────────
*.psd
*.ai
*.sketch
*.xd
*.fig

# ── Documentos ───────────────────────────────────────────────────
*.pdf
*.docx
*.doc

# ── Sistema ──────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Node ─────────────────────────────────────────────────────────
node_modules/
npm-debug.log*

# ── Ambiente ─────────────────────────────────────────────────────
.env
.env.local
.escrevaral-qa.env

# ── Editor ───────────────────────────────────────────────────────
.vscode/
.idea/
*.swp

# ── Builds ───────────────────────────────────────────────────────
dist/
build/

# ── Logs ─────────────────────────────────────────────────────────
*.log

# ── Ferramentas de IA / CLI local ────────────────────────────────
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/skills/**
.codex/
.agents/sentinel/*.log
.agents/sentinel/*.pid
.agents/sentinel/state.json
.agents/sentinel/*.tmp
.agents/sentinel-auto/*.log
.agents/sentinel-auto/*.pid
.agents/sentinel-auto/state.json
.agents/sentinel-auto/*.tmp
.agents/awake-ides/*.log
.agents/awake-ides/*.pid
.agents/awake-ides/state.json
.agents/awake-ides/*.tmp
.agents/conversas/*.md
.agents/auditorias/*.md

# ── Credenciais e chaves ─────────────────────────────────────────
*.pem
*.key
*.crt
*.p12
*.pfx
*secret*
*credentials*
*token*.json
*token*.txt
auth.json
playwright-state.json

# ── Python ───────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
venv/
.venv/
.python-version

# ── Acervos locais de livros (pesados, não vão ao repo) ──────────
Livros/
LivrosENG/
livrosPTBR/

# ── Experimentos e rascunhos locais ──────────────────────────────
dark-ensaio/
experimentos/

# ── Snapshots e provas locais ────────────────────────────────────
snapshots/

# ── Screenshots de auditoria (PNG pesados, gerados localmente) ───
reports/**/*.png
reports/**/*.jpg

# ── Marca histórica e ativos de design (movidos para assets/marca-historico) ──
assets/marca-historico/

# ── Legado não publicado ──────────────────────────────────────────
docs/_legado/
