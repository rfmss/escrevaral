name: Validação da Clareza do Produto desktop

on:
  pull_request:
    branches: [main]
    paths:
      - "css/20-product-clarity-desktop.css"
      - "css/20-product-clarity-desktop-controls.css"
      - "product-clarity-controller.js"
      - "styles.css"
      - "service-worker.js"
      - "index.html"
      - "ui-dialog.js"
      - "scripts/auditor-product-clarity-desktop.py"
      - "scripts/auditor-scriptorium-paper.py"
      - ".github/workflows/product-clarity-desktop-pr.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: product-clarity-desktop-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validar:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Clonar repositório
        uses: actions/checkout@v4.2.2

      - name: Configurar Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: "3.12"

      - name: Instalar Playwright e Chromium
        run: |
          python -m pip install --upgrade pip
          python -m pip install playwright
          python -m playwright install --with-deps chromium

      - name: Validar sintaxe
        run: |
          python -m py_compile scripts/auditor-product-clarity-desktop.py
          python -m py_compile scripts/auditor-scriptorium-paper.py

      - name: Servir branch
        run: |
          python -m http.server 8799 > /tmp/escrevaral-clarity-server.log 2>&1 &
          echo $! > /tmp/escrevaral-clarity-server.pid
          for tentativa in {1..30}; do
            curl --fail --silent http://127.0.0.1:8799/ >/dev/null && exit 0
            sleep 1
          done
          cat /tmp/escrevaral-clarity-server.log
          exit 1

      - name: Auditar clareza desktop
        run: python scripts/auditor-product-clarity-desktop.py

      - name: Auditar contraste da folha Scriptorium
        run: python scripts/auditor-scriptorium-paper.py

      - name: Guardar evidências
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: product-clarity-desktop-${{ github.run_id }}
          path: reports/auditoria/product-clarity-desktop-artifacts/
          retention-days: 14
